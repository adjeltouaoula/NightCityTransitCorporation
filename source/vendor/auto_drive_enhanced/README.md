# Auto Drive Enhanced compiler snapshot

These REDscript sources are a local compiler fixture for the ADE-dependent
0.3 development baseline. They are copied into `tmp/compile-root` only when
Auto Drive Enhanced is not installed in the game directory.

They are not included in NCTC release archives and NCTC does not overwrite the
user's installed ADE files.
