# NCTC owns this filter group and its marker variant.  A world-map filter
# enum must be one already compiled into the game; Courier is otherwise
# unused in this installation, unlike CustomFilter which Odometer also uses.
Mappins.NCTCStopVariant:
  $type: gamedataMappinVariant_Record
  enumName: CPO_PingDoorVariant

WorldMap.NCTCStopsFilter:
  $type: gamedataWorldMapFilter_Record
  enumName: Courier

WorldMap.NCTCStopsFilterGroup:
  $type: gamedataMappinUIFilterGroup_Record
  filterName: UI-MappinTypes-NCTCStops
  filterType: WorldMap.NCTCStopsFilter
  mappins:
    - Mappins.NCTCStopVariant
  # Runtime mappins registered by redscript must be collected explicitly.
  # This matches NCGT's working filter architecture; the variant above keeps
  # the result limited to NCTC markers.
  shouldCollectMappins: true

# This adds one checkbox, “Transport Corp Lines”, in the Custom tab only.
# It does not replace, append to, or recolour any vanilla filter group.
WorldMap.WorldMapCustomFiltersList.list:
  - !append-once WorldMap.NCTCStopsFilterGroup

Mappins.NCTCStopMappinDefinition:
  $type: gamedataMappinDefinition_Record
  showInWorld: false
  showOnMap: true
  showOnMinimap: false
  visibilityRange: 0
  possibleVariants:
    - Mappins.NCTCStopVariant

WorldMappinUIProfile.NCTCStop:
  $base: WorldMappinUIProfile.Quest
